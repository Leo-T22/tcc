
<div class="card">
    <div class="card-header">
    <h3>Editar página</h3>
  </div>
    <div class="card-body">
    <form action="/manager_panel/managepage/edit" method="POST">
    <label for="title">Título: </label>
        <input type="text" id="title" name="title" placeholder="Título" class="form-control" value="{{pages.title}}">
    <label for="content">Conteúdo: </label>
        <textarea name="content" class="form-control"></textarea value="{{pages.content}}">
    <br><button type="submit" class="btn btn-success">Editar página</button>
    </form>
</div>
</div>